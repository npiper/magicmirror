.runtimebuild:
  script:
    - !reference [.init, script]
    - |
      set -e
      echo "Building debian image ..."
      [[ "${FAT}" != "_fat" ]] && export SLIM="-slim"
      [[ "${FAT}" == "_debug" ]] && export NODE_ENV="test" || export NODE_ENV="production"
      if [[ "${CI_COMMIT_BRANCH}" == "master" ]]; then
        export DEB_IMAGE="${DEB_IMAGE_MASTER}"
      else
        export DEB_IMAGE="${DEB_IMAGE_DEVELOP}"
      fi
      export BUILDKIT_VARS="DEB_IMAGE GIT_INFO CI_REGISTRY_IMAGE BUILDER_TAG SLIM CI_PROJECT_ID FAT NODE_ENV"
      if [[ "${FAT}" == "_debug" ]]; then
        export TARGETPLATFORM="linux/amd64"
      else
        export TARGETPLATFORM="linux/amd64,linux/arm64,linux/arm/v7"
      fi
      set | grep -E "DEB_IMAGE=|CI_REGISTRY_IMAGE=|BUILDER_TAG=|FAT="

build_debian_slim:
  stage: build
  variables:
    CNTNR_FILE_NAME: "Dockerfile-debian"
    FAT: "_default"
  needs:
    - "bld_art: [amd64_default]"
    - "bld_art: [arm64_default]"
    - "bld_art: [arm_default]"
  extends:
    - .rule
  environment:
    name: "$CI_COMMIT_BRANCH/Build Debian slim"
  script:
    - !reference [.runtimebuild, script]
    - |
      if [[ "${CI_COMMIT_BRANCH}" == "master" ]]; then
        export PUSH_IMAGES="${CI_REGISTRY_IMAGE}:${CI_COMMIT_BRANCH},${DOCKER_USER}/magicmirror:latest,${DOCKER_USER}/magicmirror:${MAGICMIRROR_VERSION}"
      else
        export PUSH_IMAGES="${CI_REGISTRY_IMAGE}:${CI_COMMIT_BRANCH},${DOCKER_USER}/magicmirror:${CI_COMMIT_BRANCH}"
      fi
      build

build_debian_fat:
  stage: build
  variables:
    CNTNR_FILE_NAME: "Dockerfile-debian"
    FAT: "_fat"
  needs:
    - "bld_art: [amd64_fat]"
    - "bld_art: [arm64_fat]"
    - "bld_art: [arm_fat]"
  extends:
    - .rule
  environment:
    name: "$CI_COMMIT_BRANCH/Build Debian fat"
  script:
    - !reference [.runtimebuild, script]
    - |
      if [[ "${CI_COMMIT_BRANCH}" == "master" ]]; then
        export PUSH_IMAGES="${DOCKER_USER}/magicmirror:fat,${DOCKER_USER}/magicmirror:${MAGICMIRROR_VERSION}_fat"
      else
        export PUSH_IMAGES="${DOCKER_USER}/magicmirror:${CI_COMMIT_BRANCH}_fat"
      fi
      build

.build_debug:
  stage: build
  variables:
    CNTNR_FILE_NAME: "Dockerfile-debian"
    FAT: "_debug"
    PUSH_IMAGES: "${CI_REGISTRY_IMAGE}:${CI_COMMIT_BRANCH}_debug${NODE}"
  needs:
    - "bld_art: [amd64_debug]"
  extends:
    - .rulenomaster
  environment:
    name: "$CI_COMMIT_BRANCH/Build Debug"
  script:
    - !reference [.runtimebuild, script]
    - build

build_debug_node_dev:
  variables:
    NODE: "_curr"
  extends:
    - .build_debug

build_debug_node:
  variables:
    NODE: "_lts"
  extends:
    - .build_debug
